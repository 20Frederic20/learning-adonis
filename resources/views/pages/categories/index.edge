<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title> AdonisJS - A fully featured web framework for Node.js </title>

  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>

<body>
  @include('partials/header')
  <form class="m-16 p-16" action="/categories" method="POST">
    {{ csrfField() }}
    <div class="space-y-12 m-auto">
      <table>
        <thead>
          <th>Id</th>
          <th>Slug</th>
          <th>Nom</th>
        </thead>
        <tbody>
          @each( value in categories)
          <tr>
            <td> {{ value.id }} </td>
            <td> {{ value.slug }} </td>
            <td> {{ value.name }} </td>
          </tr>
          @end
        </tbody>
      </table>
    </div>
  </form>
</body>

</html>